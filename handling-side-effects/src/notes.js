/**
 * 
 Side effects are Task that needs to be executed in app but don't impact the current component render cycle

//-- useCallback hook to do not render or run specific task until dependency get changed

// and use callback hook as when passing function as dependency

// useCallback also use dependency 
 */
